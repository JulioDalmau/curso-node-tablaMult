# Notas:
Mi primer programa en NodeJS

```
Options:
    --help     Showhelp                                              [boolean]
    --version  Show version number                                   [boolean]
-m, --mult     Es la base de la tabla de multiplicar       [number] [required]
-l, --listar   Muestra la tabla en consola          [boolean] [default: false]
-h, --hasta    Hasta que número debe multiplicar        [number] [default: 10]
```